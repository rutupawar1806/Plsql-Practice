--Select into

DECLARE 
E_NAME VARCHAR(20);

BEGIN
    SELECT FIRST_NAME INTO E_NAME 
    FROM HR.EMPLOYEES
    WHERE EMPLOYEE_ID = 100;
    DBMS_OUTPUT.PUT_LINE(F_NAME);

END;


--Select into when we want to fetch from two diff Coloumn
DECLARE 
E_NAME VARCHAR(20);
E_SALARY NUMBER;

BEGIN
    SELECT FIRST_NAME, SALARY INTO E_NAME, E_SALARY FROM HR.EMPLOYEES
    WHERE EMPLOYEE_ID = 100;
    DBMS_OUTPUT.PUT_LINE(E_NAME || ' has Salary ' || E_SALARY);

END;