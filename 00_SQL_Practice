create Databases HPS_Company;

use HPS_Company;

create table Employees(
  Id int,
  Name varchar,
  Salary number);
  
Insert into Employees(id, name, salary) 
  values
  (01,'Rutuja', 800000),
  (02,'Kshitu', 900000),
  (03,'Kiara', 1200000),
  (04,'Ruhi', 500000),
  (05,'Tara', 700000);
  
select * from Employee
where salary>800000;

alter table Employee
add Department Varchar(20);

update Employee
set Department = 'IT'
where ID in (1,3,5);

update Employee
set department = 'Medical'
where id in (2,4);

select * from Employee
where Department = 'IT';

SELECT DISTINCT Department
from Employee;

select * from employee
where salary BETWEEN  500000 and 1000000;

select * from employee
where name like '%u';

select * from employee
order by salary desc;*/

select sum(salary) as total_salary
from employee;

select avg(salary) as avg_salary
from employee;

select highest(salary) as highest_salary
from employee;